import { ArgTypes, Meta, Stories, Title } from '@storybook/blocks';
import * as AnimatedNumberStories from './AnimatedNumber.stories';

<Meta of={AnimatedNumberStories} />

<Title>AnimatedNumber Component</Title>

Component animates number count from start number to end number.

Start can be less than or greater tthan end number.

Rendered as `<span>` element, should be styled within parent component, page, or layout.

## API Reference

<ArgTypes of={AnimatedNumberStories} />

## Examples

<Stories />